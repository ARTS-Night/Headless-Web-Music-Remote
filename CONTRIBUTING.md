# Feedback policy

HWMR v0.1.0 is in feedback and validation mode. Until results from roughly 3–5 independent environments are collected, new features are generally deferred.

Reproducible bugs found in real use should receive the smallest practical fix in v0.1.1. Feature requests are welcome and will be collected in Issues rather than immediately implemented.
